apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keycloak-forward.fullname" . }}-test-connection"
  labels:
    {{- include "keycloak-forward.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keycloak-forward.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
