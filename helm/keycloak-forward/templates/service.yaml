apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloak-forward.fullname" . }}
  labels:
    {{- include "keycloak-forward.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keycloak-forward.selectorLabels" . | nindent 4 }}
